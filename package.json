{
  "name": "nemorize",
  "type": "module",
  "scripts": {
    "build": "yarn exec eleventy --input=./src --output=./_site",
    "serve": "yarn build --serve"
  },
  "packageManager": "yarn@4.5.3",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^6.0.0"
  }
}
